
server:
  port: 8081

spring:
 datasource:
  url: jdbc:postgresql://postgres:5432/sgdea
  username: sgdea
  password: sgdea
 jpa:
  hibernate:
   ddl-auto: update

 kafka:
  bootstrap-servers: kafka:9092

logging:
 level:
  root: INFO
 pattern:
  level: "%5p [traceId:%X{traceId:-N/A}] [otelTrace:%X{trace_id:-N/A}] [span:%X{span_id:-N/A}]"
